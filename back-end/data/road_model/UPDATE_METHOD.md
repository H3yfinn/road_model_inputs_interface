# Road model numeric data update method log

Use this file to record how numeric source files in `back-end/data/road_model/` are produced or updated.

## Current seed defaults

- Date: 2026-06-02
- Author: Codex, with user review required
- Change summary: Initial seed CSV files for Module 1 default vehicle types, drive shares, valid drive combinations, mileage, efficiency, and scalar assumptions.
- Source inputs (files/links): Existing literal defaults in `back-end/core/road_module1_defaults.py` before the CSV-backed loading change.
- Generation method (script/notebook path and version): Manual extraction from the previous Python constants into CSV source files. No upstream generator exists yet for these seed files.
- Output files changed:
  - `road_module1_default_vehicle_types.csv`
  - `road_module1_default_drive_shares.csv`
  - `road_module1_valid_drives_by_vehicle_type.csv`
  - `road_module1_default_mileage_km_per_year.csv`
  - `road_module1_default_efficiency_mj_per_km.csv`
  - `road_module1_default_assumptions.csv`
- Validation checks run: Import smoke check for `core.road_module1_defaults`; `back-end/scripts/audit_road_model_data_sourcing.py`.
- Notes/limitations: These are provisional seed defaults. They are not generated by `../leap_transport/` yet and should be replaced or regenerated by a documented notebook/workflow when the upstream input-building methods are ready.

## Required entry template

- Date:
- Author:
- Change summary:
- Source inputs (files/links):
- Generation method (script/notebook path and version):
- Output files changed:
- Validation checks run:
- Notes/limitations:

## Example (placeholder for future leap_transport pipeline)

- Date: TBD
- Author: TBD
- Change summary: Generate `leap_import_workbooks` from upstream transport outputs.
- Source inputs (files/links): `../leap_transport/...`
- Generation method (script/notebook path and version): TBD (from `../leap_transport/`)
- Output files changed: `back-end/data/road_model/leap_import_workbooks/*`
- Validation checks run: schema columns present, row keys unique, no missing required economies.
- Notes/limitations: initial implementation; refine mappings after taxonomy review.
